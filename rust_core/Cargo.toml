[package]
name = "vietnamese_ai_rust"
version = "0.1.0"
edition = "2021"

[lib]
name = "vietnamese_ai_rust"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22.0", features = ["extension-module"] }
rayon = "1.8" # Hỗ trợ tính toán đa luồng
